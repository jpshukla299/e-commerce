import React from 'react'
import ReactStars from "react-rating-stars-component";


const Product = (props) => {
    const{name, img, price, seller,stock, shipping,features,star} = props.product;

    // let{name,img,star,price,shipping} = props.product
  return (Product)
   
}

export default Product